/**
 * users-permissions plugin extension.
 *
 * The custom `user` schema is merged automatically from
 * `content-types/user/schema.json`.
 *
 * Custom auth/registration/role logic will be added here later.
 */

export default (plugin: any) => {
  return plugin;
};
